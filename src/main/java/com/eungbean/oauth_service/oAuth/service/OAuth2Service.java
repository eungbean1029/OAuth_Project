package com.eungbean.oauth_service.oAuth.service;

import com.eungbean.oauth_service.oAuth.OAuth2LoginRequset;
import com.eungbean.oauth_service.user.dto.UserLoginResponse;

public class OAuth2Service {

    public UserLoginResponse login(OAuth2LoginRequset params) {
//        OAuth2UserInfo oAuth2UserInfo = get(params);
//        return jwtTokenGenerator.generateAccessToken(findUser(oAuth2UserInfo));
        return null;
    }
}
